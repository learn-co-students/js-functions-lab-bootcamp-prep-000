// create function happyHolidays that accepts no argument and returns "Happy holidays!" as a string
function happyHolidays() {
  return "Happy holidays!"
};

// create function happyHolidaysTo that accepts one argument (name), and returns `Happy holidays, ${name}!` as a string
function happyHolidaysTo(name) {
  return `Happy holidays, ${name}!`
};

//  create function happyHolidayTo that accepts two arguments (holiday,name) and returns `Happy ${holiday}, ${name}!` as a string
function happyHolidayTo(holiday, name) {
  return `Happy ${holiday}, ${name}!`
};

// create function named holidayCountdown that accepts two arguments (holiday,days) and returns `It's ${days} until ${holiday}!`
function holidayCountdown(holiday, days) {
  return `It's ${days} until ${holiday}!`
};