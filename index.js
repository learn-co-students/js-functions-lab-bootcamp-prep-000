/* 
1. Declare a function named happyHolidays
2. When call function, return string "Happy holidays!".
*/
function happyHolidays() {
  return "Happy holidays!";
  }

/* 
1. Declare a function named happyHolidaysTo.
2. Function accepts parameter named name, whose value is string.
3. When call function, return the string `Happy holidays, ${name}!`.
*/
function happyHolidaysTo(name) {
  return "Happy holidays, you!";
}

/* 
1. Declare a function named happyHolidaysTo.
2. Function accepts 2 parameters named holiday and name, whose values are strings.
3. When call function, return string `Happy ${holiday}, ${name}!`.
*/
function happyHolidayTo(holiday, name) {
  return "Happy Independence Day, you!;"
}

/* 
1. Declare a function named holidayCountdown.
2. Function accepts 2 parameters named days, holiday, whose values are a number and string. 
3. When call function, return string `It's ${days} days until ${holiday}!`.
*/
function holidayCountdown(days, holiday) {
  return `It is ${days} days until ${holiday}!`;
}
