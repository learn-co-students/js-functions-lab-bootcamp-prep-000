// write your code below!
function happyHolidays(){                             //function that returns Happy holidays!
  return ("Happy holidays!")
}

function happyHolidaysTo(name){                     //funciton that takes the name and returns Happy holidays, 'name'
  return (`Happy holidays, ${name}!`)
}

function happyHolidayTo(holiday, name){           //function that takes holiday and name and returns "Happy 'holiday', 'name'"
  return (`Happy ${holiday}, ${name}!`)
}

function holidayCountdown(holiday, days){           //function that takes holiday and value for days and makes a string that reads "It's 'days' days until 'holiday'"
  return (`It\'s ${days} days until ${holiday}!`)
}
